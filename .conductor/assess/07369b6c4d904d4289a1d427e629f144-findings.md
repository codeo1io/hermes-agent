# Adversarial repository assessment — run b45c23b895be / attempt 07369b6c4d904d4289a1d427e629f144

Tree: `conductor/run-b45c23b895be` @ `bcf55bbaed` (clean). Scope: fresh adversarial pass over
correctness, reliability, security, performance, maintainability, tests, docs, DX, architecture.
Deduped against `.hermes/conductor-boundary-roadmap.md` (ROADMAP.md, 1,124 lines, rm-001..rm-019)
and prior known-class findings (planned-stop, god-file class, stale README, test litter,
bare skipif, untested modules). Every finding below carries current file/line evidence.

## Findings

### F-A [HIGH] [supply-chain/policy] Live mutable `@main` ref in CI: private-leak-sentinel
- `.github/workflows/private-leak-sentinel.yml:18` — `uses: codeo1io/.github/.github/workflows/private-leak-sentinel.yaml@main`
- Sole unpinned `uses:` across all 39 workflow files (every other ref is a 40-char SHA + `# vN`).
- Violates the repo Dependency Pinning Policy (AGENTS.md). Introduced in `fcc14f9a8e` — *after*
  the roadmap had already recorded the class (rm-019, plugin-validate docs snippet), making this
  a fresh live instance, not a dupe.
- Whoever controls `codeo1io/.github@main` owns the workflow content that runs gitleaks + repo
  checkout on self-hosted runners on every push/PR/schedule.

### F-B [MEDIUM] [test-infra/reliability] #93911 budget mirror never runs on py-only PRs (lane gap)
- `apps/desktop/src/plugins/hermes-bots/relay-deliver-budget.test.ts:13-16` (vitest) reads
  `hermes_cli/config_defaults.py` + `tools/bot_relay.py` and is the ONLY enforcement that TS
  relay constants (`relay.ts:54-64`) mirror the Python budget constants (its own header says
  exactly this; `tests/tools/test_bot_relay.py:153-167` checks only Python-internal consistency).
- `.github/workflows/ci.yaml:107` gates js-tests on `needs.detect.outputs.frontend == 'true'`;
  `scripts/ci/classify_changes.py:230-233` sets `frontend` only for frontend-tree files.
- So a PR touching only `tools/bot_relay.py` or `hermes_cli/config_defaults.py` (e.g. raising
  `TURN_ATTEMPT_TIMEOUT_SECONDS`) skips the js lane → mirror drift lands **green on PR**;
  caught only post-merge (push classifier fails open, ci.yaml:29-30) — the exact
  "green on PR, red on main" failure mode AGENTS.md warns about.
- The inverse mechanism already exists (`_PY_RELEVANT_CONTRACT_FILES`, classify_changes.py:98)
  but no JS-direction contract list covers these .py files.

### F-C [MEDIUM] [docs/DX] Guide teaches the `.env` pattern the code treats as stale residue
- `website/docs/guides/tips.md:184-189` — "Set `TERMINAL_ENV=docker` in your `.env`" +
  `TERMINAL_DOCKER_IMAGE=hermes-sandbox:latest`.
- Behavioral, non-secret config in the secrets-only file; config.yaml owns `terminal.*`
  (`hermes_cli/config.py:2014-2016` bridges `backend`→`TERMINAL_ENV`), and
  `hermes_cli/env_loader.py:395-399` explicitly neutralizes "a stale TERMINAL_ENV=docker left
  in ~/.hermes/.env". Docs recommend what the code actively cleans up.

### F-D [LOW-MEDIUM] [maintainability] `agent/auxiliary_client.py` = 7,630 lines / 376 functions
- 3.8× the repo's ~2,000-line split signal; largest function is 128 lines (none >300), so this
  is a file-level split case, not a rm-001 hot-function case (rm-001's list doesn't name it).
- Fresh instance data; flagged as overlapping the previously-reported god-file *class*.

### F-E [LOW] [DX/process] Sentinel has no `workflow_dispatch` → empty retrigger commits
- `.github/workflows/private-leak-sentinel.yml:6-12` — triggers are push/PR/schedule only;
  retriggering after the external fix required the empty commit `bcf55bbaed`
  (verified empty via `git cat-file -p`: no file changes).

### F-F [LOW] [maintainability] Dead parameter in fresh code
- `tools/session_search_tool.py:113` — `_parse_iso_bound(..., *, as_exclusive_end=False)`:
  documented (:117) and passed at :589 but never used in the body (SQL `<` is already exclusive).

### F-G [LOW] [correctness edge] Silent truncation of `exclude_session_ids` at 20
- `tools/session_search_tool.py:34,160` — cap is deliberate (runaway-list guard, :33) but
  truncation is silent: >20 model-supplied ids drop exclusions with no signal in tool output,
  so hits from excluded sessions can reappear.

## Verified-healthy surfaces (negative results, checked this session)
- Session-search SQL (rm-005): fully parameterized incl. limit/offset; all 4
  `_search_filter_clauses` call sites join `sessions s`; `_get_session_meta` degrades to `{}`.
- Cron adopt-guard (PR #34): ledger-based disposition, predicate tests green, design sound.
- Web file routes: `_resolve_managed_path` (web_server_files.py:141) rejects `..` pre- and
  post-resolution, canonical-path root containment, symlink-safe; media route resolves + root-checks.
- `scripts/validate_plugin_catalog.py plugin-catalog/` → OK: 137 files valid (rm-004 healthy).
- pyproject.toml: zero bare `>=` deps without upper bound (tomllib scan of project + extras).
- `scripts/check_compat_pointers.py` clean; `os.kill` audit all canonical/documented;
  image-gen `requests.*` calls all carry timeouts; 1 naive `utcnow` (temp filename, harmless);
  no mutable default args in prod; `scripts/ci/` SHA pins otherwise complete.

## Provisional future work
- implement: pin the sentinel `uses:` to a SHA + `# vN` (and add `workflow_dispatch`).
- implement: add `_JS_RELEVANT_CONTRACT_FILES` = {tools/bot_relay.py, hermes_cli/config_defaults.py}
  forcing `frontend=true` in classify_changes.py.
- implement: rewrite tips.md security section to config.yaml `terminal.backend` / `docker_image`.
- implement: split auxiliary_client.py; drop `_parse_iso_bound` dead param; surface the
  exclude-cap truncation in the tool's output.
